{
  "name": "claude-code-ui",
  "version": "1.0.0",
  "description": "Claude Code UI Desktop",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder"
  },
  "build": {
    "appId": "com.claudecodeui.desktop",
    "productName": "ClaudeCodeUI",
    "directories": {
      "output": "release"
    },
    "asar": false,
    "files": [
      "main.js",
      {"from": "app/dist", "to": "app/dist"},
      {"from": "app/dist-server", "to": "app/dist-server"},
      {"from": "app/node_modules", "to": "app/node_modules", "filter": ["**/*", "!.cache/**"]}
    ],
    "win": {
      "target": "portable"
    },
    "portable": {
      "artifactName": "ClaudeCodeUI.exe"
    }
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8"
  }
}
